<%= form_with(model: quote, local: true) do |form| %>
  <% if quote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quote.errors.count, "error") %> prohibited this quote from being saved:</h2>

      <ul>
      <% quote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :quote_name %>
  </div>

  <div class="field">
    <%= form.label :abbreviated_quote %>
    <%= form.text_field :abbreviated_quote, id: :quote_abbreviated_quote %>
  </div>

  <div class="field">
    <%= form.label :quote %>
    <%= form.text_area :quote, id: :quote_quote %>
  </div>

  <div class="field">
    <%= form.label :file %>
    <%= form.file_field :file, id: :quote_file %>
  </div>

  <div class="field">
    <%= form.label :genre %>
    <%= form.text_field :genre, id: :quote_genre %>
  </div>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link, id: :quote_link %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
